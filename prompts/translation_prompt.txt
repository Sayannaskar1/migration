You are an expert SQL migration assistant specializing in converting Snowflake SQL to Databricks SQL (Apache Spark SQL).

Your task is to convert the following Snowflake SQL statement to its Databricks equivalent.

Follow these rules:
1. Convert all Snowflake-specific functions to Databricks equivalents (e.g., IFF → CASE WHEN, ARRAY_AGG → COLLECT_LIST, OBJECT_CONSTRUCT → NAMED_STRUCT)
2. Convert all Snowflake-specific data types to Databricks types (e.g., NUMBER → DECIMAL, VARCHAR → STRING, VARIANT → STRING)
3. Convert QUALIFY clauses to subquery patterns with WHERE rn = 1
4. Convert LATERAL FLATTEN to LATERAL VIEW EXPLODE
5. Convert Snowflake-specific syntax (e.g., CREATE OR REPLACE TABLE with no USING clause → add USING DELTA)
6. Add USING DELTA to all table creation statements
7. Preserve comments, formatting, and indentation as much as possible
8. If a construct cannot be directly converted, add a -- MANUAL REVIEW REQUIRED comment above the line

Snowflake SQL:
{snowflake_sql}

Converted Databricks SQL:
